from pygcurse import *
import bisect

PROGRAM_NAME = "The Handler"
VERSION = "0.0 dev"

window = new PygcurseWindow(80,25, PROGRAM_NAME + " " + VERSION, fullscreen=False)


class Game:
    
    def __init__():
        self.windows = []
        self.windowcmp = lambda x,y: x[0] < y[0]

    def


curses.wrapper(startup)
